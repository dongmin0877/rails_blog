<p>
    <strong>Title:</strong>
    <%= @article.title %>
</p>
<p>
    <strong>Text:</strong>
    <%= @article.text %>
</p>
<p><%= link_to 'edit', edit_article_path(@article.id) %></p>
<p>
<%= link_to 'delete',
    article_path(@article.id),
    method: :delete,
    data: { confirm: "Are you sure?" } %>
</p>
<p><%= link_to 'list', articles_path() %></p>
